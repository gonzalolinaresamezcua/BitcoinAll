# Copia a data/bitcoin.conf al arrancar el nodo por primera vez:
#   mkdir -p data && cp bitcoin.conf.example data/bitcoin.conf

server=1
txindex=1
wallet=primera
listen=1
minrelaytxfee=0.01
incrementalrelayfee=0.01
